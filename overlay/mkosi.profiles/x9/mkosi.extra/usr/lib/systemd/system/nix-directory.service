[Unit]
Description=Create the persistent Nix store directory
DefaultDependencies=no
RequiresMountsFor=/var
After=local-fs-pre.target
Before=nix.mount local-fs.target

[Service]
Type=oneshot
ExecStart=/usr/bin/install -d -m 0755 /var/lib/nix
RemainAfterExit=yes
